import { styled } from '@mui/material'

export const BuxInfoSectionWrapperStyled = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  gap: '30px',
})

export const TopViewStyled = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  gap: '4px',
  alignItems: 'center',
})
